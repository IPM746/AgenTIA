Run the tests with:

npx tsx math.test.ts

Identify the failure and fix the problem so that the intended behavior of multiply is correct.

Run the tests again with:

npx tsx math.test.ts

Verify that they pass.